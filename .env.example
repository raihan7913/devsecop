# =============================================
# Backend Environment Variables (backend/.env)
# =============================================

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration - WAJIB GANTI di production!
# Generate dengan: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-this-in-production-32chars
JWT_EXPIRES_IN=24h

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Rate Limiting (Development - lebih longgar untuk testing)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=500

# Database
DB_PATH=./academic_dashboard.db

# =============================================
# Frontend Environment Variables (frontend/.env)
# =============================================

# REACT_APP_API_BASE_URL=http://localhost:5000
# GENERATE_SOURCEMAP=false

# =============================================
# PRODUCTION NOTES (Railway)
# =============================================
# Di Railway, set environment variables berikut:
# - NODE_ENV=production
# - PORT=5000 (atau Railway akan auto-set)
# - JWT_SECRET=<random-32-chars>
# - FRONTEND_URL=https://your-app.railway.app
# - RATE_LIMIT_WINDOW_MS=900000
# - RATE_LIMIT_MAX_REQUESTS=500
